import { TestimonyDirective } from './testimony.directive';

describe('TestimonyDirective', () => {
  it('should create an instance', () => {
    const directive = new TestimonyDirective();
    expect(directive).toBeTruthy();
  });
});
